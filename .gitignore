# Cargo build artifacts (per-challenge and workspace root)
/target/
**/target/

# Backup files cargo/rustfmt sometimes leave behind
**/*.rs.bk

# Editor/IDE
.vscode/
.idea/
*.swp

# macOS
.DS_Store

# Env files (if any challenge needs local config/secrets, e.g. http-server tests)
.env

# Local test fixtures/data per challenge — never committed
**/test/
